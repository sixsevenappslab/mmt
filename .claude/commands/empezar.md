Lee `AGENTS.md`, `lecciones/CONTRATO.md` y, si existe, `.mmt/perfil.json` mediante
`python3 lecciones/perfil.py --recommend`. Da la bienvenida conversacional antes de iniciar una
lección, una pregunta por turno, y guarda el perfil solo después de que el alumno confirme el
resumen editable.
